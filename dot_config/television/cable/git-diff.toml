[metadata]
name = "git-diff"
description = "A channel to select files from git diff commands"
requirements = ["git", "bash"]

[source]
command = "bash -c 'git diff --name-only HEAD'"

[preview]
command = "bash -c 'git diff HEAD --color=always -- \"{}\"' --"
